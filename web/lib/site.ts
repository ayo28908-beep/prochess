// Site-wide configuration — everything the site links out to lives here.

// ChessStream Africa — live chess broadcasting platform (subsidiary of Prochess).
// In production this will be chessstream.africa; in dev it runs on port 3001.
export const STREAM_URL = "http://127.0.0.1:3001";

export const STREAM_NAME = "ChessStream Africa";
